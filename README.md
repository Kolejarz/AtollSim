# Atoll Navigator

Initial commit — repository scaffold.

This repository contains Atoll Navigator, a sliding-maze puzzle and economy simulator built as a React component. The full source will be added to `src/` and a simple Pages site will be published from `docs/`.
